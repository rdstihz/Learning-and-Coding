#include <bits/stdc++.h>

using namespace std;

const int maxn = 110;

int a[maxn][maxn];

int main() {

    int T;
    scanf("%d%d", &T);
    while (T--) {
        int n, m;
        for (int i = 1; i <= n; i++)
            for (int j = 1; j <= m; j++) {
                scanf("%d", &a[i][j]);
            }
        int ans = 0;
        for(int i = 1; i <= n; i++) {
            for(int j = 1; j <= m; j++) {}
        }
    }

    return 0;
}