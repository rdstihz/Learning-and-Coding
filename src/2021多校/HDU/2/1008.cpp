#include <bits/stdc++.h>

using namespace std;
const int maxn = 150000 + 100;
map<string, int> id;
string s;



int main() {
    ios::sync_with_stdio(false);
    int T;
    cin >> T;
    while (T--) {
        int n;
        cin >> n;
        for (int i = 1; i <= n; i++) {
            cin >> s;
            id[s] = i;
        }
        int m;
        cin >> m;
        for(int i = 1; i <= m; i++) {

        }
    }

    return 0;
}